set(base_examples
    p4-topo-test
    p4-echo-server
)
foreach(
  example
  ${base_examples}
)
  build_lib_example(
    NAME ${example}
    SOURCE_FILES ${example}.cc
    LIBRARIES_TO_LINK
      ${libcsma}
      ${libinternet}
      ${libapplications}
  )
endforeach()